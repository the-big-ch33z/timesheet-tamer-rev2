
import '@testing-library/jest-dom';

// This file ensures TypeScript recognizes the Jest DOM matchers
// It extends the Jest expect interface with the DOM matchers
// The import above automatically registers these matchers
